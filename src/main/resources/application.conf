ktor {
    deployment {
        port = 5050
    }

    application {
        modules = [ commithor.ApplicationKt.main ]
    }
}

commithor {
    version = "1.0-SNAPSHOT"
    repository = "https://github.com/ratpack/example-books.git"
    tempDir = "/tmp/repo"
}